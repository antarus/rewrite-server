logging:
  config: classpath:logback.xml

spring:
  main:
    banner-mode: 'off'

server:
  port: 0

rewrite:
  server:
    config-directory: '${java.io.tmpdir}/rewrite/${random.uuid}/config'
    work-directory: '${java.io.tmpdir}/rewrite/${random.uuid}/repository'
    mvn-path: /home/cedric/.sdkman/candidates/maven/current/bin/mvn
